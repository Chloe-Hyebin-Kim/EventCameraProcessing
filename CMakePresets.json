{
  "version": 3,
  "cmakeMinimumRequired": { "major": 3, "minor": 21, "patch": 0 },
  "configurePresets": [
    {
      "name": "windows-qt",
      "displayName": "Windows (Qt + bundled OpenCV)",
      "description": "Open this repo folder in Visual Studio ('File > Open > Folder') to use this preset. Requires the QT_DIR environment variable to point at your Qt kit (e.g. C:\\Qt\\6.7.0\\msvc2022_64) - set it as a Windows environment variable and restart Visual Studio. OpenCV is picked up automatically from the bundled ocv440/ by CMakeLists.txt, no variable needed here.",
      "generator": "Ninja",
      "binaryDir": "${sourceDir}/build/${presetName}",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Release",
        "CMAKE_PREFIX_PATH": "$env{QT_DIR}"
      },
      "condition": {
        "type": "equals",
        "lhs": "${hostSystemName}",
        "rhs": "Windows"
      }
    },
    {
      "name": "x64-Debug",
      "displayName": "x64-Debug (Windows, Release-forced)",
      "description": "Visual Studio가 'File > Open > Folder'로 열 때 자동으로 제안하는 기본 프리셋 이름과 똑같이 정의해둔 것. Metavision SDK는 Windows용 Release 바이너리만 배포하므로, VS의 진짜 Debug(/MDd) 구성으로 이 프리셋이 뜨는 일이 없도록 windows-qt(Release)를 그대로 상속한다. 이름만 x64-Debug이고 실제로는 Release로 빌드된다 - CMakeLists.txt의 강제 Release 로직과 이중 방어.",
      "inherits": "windows-qt",
      "binaryDir": "${sourceDir}/out/build/${presetName}"
    },
    {
      "name": "x64-Release",
      "displayName": "x64-Release (Windows)",
      "inherits": "windows-qt",
      "binaryDir": "${sourceDir}/out/build/${presetName}"
    },
    {
      "name": "linux",
      "displayName": "Linux",
      "generator": "Ninja",
      "binaryDir": "${sourceDir}/build/${presetName}",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Release"
      },
      "condition": {
        "type": "equals",
        "lhs": "${hostSystemName}",
        "rhs": "Linux"
      }
    }
  ]
}
